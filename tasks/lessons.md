# Lessons Learned

_Append an entry here after any correction from Shane._
_Format: [YYYY-MM-DD] | what went wrong | rule for next time_
_Read this file at the START of every session and apply all rules before touching any code._

[2026-04-27] | Proposed a unique index on posts(clip_code, platform, stat_date) for the long-form sync, which would have collided with existing Shorts rows where many clip_details_code values share the same clip_code (e.g. MBM016 has 12 clips). | Before adding any unique index to `posts`, enumerate which content_type rows occupy the table. Long-form rows have NULL clip_details_code, so prefer a PARTIAL unique index keyed on a column that is unique per row (content_id) and scoped with WHERE content_type='long_form'. Don't reuse Shorts' conflict key for long-form.
[2026-04-28] | Local Playwright scraper at scripts/youtube-studio-sync.ts was the actual source of daily Shorts data, not the Vercel cron — but it broke when YouTube Studio changed their CSV export DOM around Apr 24. | Before assuming a Vercel cron is broken because of stale data, check whether a local LaunchAgent or other scheduled job is the actual data source. Search for .plist files and scripts/ entries.
[2026-04-29] | Asked user to run the full youtube-studio-sync.ts script to verify a CSV-export selector fix; the script's full pipeline writes to the posts table, so the diagnostic run incidentally re-populated posts with 840 rows of scraper-format data (zero-padded dailies) that will linger until the next Vercel cron at 14:00 UTC overwrites them. | When fixing a tool's selector or a single step for diagnostic purposes, do not run the tool's full pipeline. Either (a) add/use a --dry-run flag that exits before any DB write, or (b) test the affected step in isolation (e.g. a small script that only opens the page and clicks the selector, no upsert). Always reason about side effects before recommending a verification command, especially for scripts that write to shared state like Supabase.
[2026-05-05] | tried fast-forward merge of coder worktree branch into main, but the worktree base (773c7f8) was several commits behind main's HEAD (cdf553e) so FF was impossible | when merging a coder-agent worktree branch back to main, default to cherry-pick — worktrees branch off at agent dispatch time and lag main if other commits land before review
[2026-05-06] | spent ~30min diagnosing OAuth refresh token theory for an apparent "4-day long-form data gap"; manual cron trigger succeeded immediately, proving the gap was Vercel Hobby cron unreliability + YouTube Analytics' inherent ~3-day reporting lag, not auth or code | for stale-data investigations, manually trigger the relevant cron route FIRST before deep-diving theories — Hobby cron occasionally skips runs, and the 1500-day lookback self-heals on next successful run
[2026-05-12] | Shane said "copy Founder Report's deselection logic over" to fix a Dashboard "two pills highlighted" claim, but a diff showed the two preset-pill blocks in DateFilterBar.tsx and FounderReportView.tsx were byte-equivalent — there was nothing to copy | when a user frames a change as "copy from sibling X to fix Y", diff X and the target first; if identical, surface the finding (likely stale CDN bundle, browser cache, or a different UI element being misread) before writing code — don't make a no-op change to match the framing
[2026-05-14] | Reported "VIDEO_MAP is imported by these files" based on `grep -rn "VIDEO_MAP"` — but each file had its own local duplicate copy, not a real import. Sent the Phase 5 audit into a phantom migration before Shane re-checked the premise | when auditing dependencies, grep for IMPORT STATEMENTS (`from '...module'` / `import.*from`) — not for occurrences of the symbol. Generic occurrence-grep conflates local copies, type references, and imports
[2026-05-14] | Added DASHBOARD_SECRET auth gate to /api/founder-report without checking server-to-server callers. Reviewer caught that /api/diagnostics internally fetches /api/founder-report?window=30 and would have 401'd after deploy, breaking the internal consistency check | when adding an auth gate to a route, grep for ALL fetches to it — client (`/components/views/`) AND server-to-server (other `route.ts` files). Server-to-server callers need the unprefixed env var (`DASHBOARD_SECRET`), not `NEXT_PUBLIC_*`
[2026-05-14] | Marked the C4 reviewer concern about partial-unique-index ON CONFLICT as PASS based on "long-form uses similar pattern" reasoning. In reality, long-form uses `.insert(...)` not `.upsert(..., { onConflict })` — the partial index there is only a write-time enforcer, never an ON CONFLICT target. The bug only surfaced in prod (PostgREST 400 BAD REQUEST) | when a reviewer flags concern about library/framework behavior, verify against the specific code path — don't extrapolate from "similar pattern works elsewhere." Especially for ORMs/query builders where call-shape determines underlying behavior (insert vs upsert vs onConflict semantics)
[2026-05-15] | New tables added via migration (instagram_auth, instagram_comments, instagram_discovery_audit) defaulted to RLS-enabled-no-policies, silently blocking the anon client. IG cron returned 500 "No instagram_auth row found" despite the row existing. Spent a deploy cycle diagnosing. | After any new-table migration, audit RLS posture in the same diagnostic step (`relrowsecurity` + `pg_policy`). If RLS-blocked and the writer is server-only, use service-role — module-level in server-only files, lazy inside the helper in mixed files like db.ts (frontend imports it).
[2026-05-15] | supabase-js `.not('col', 'is', null)` returned [] from the Vercel runtime against the newly-added `clip_details.instagram_content_id` column, even though the equivalent PostgREST URL `?col=not.is.null` returned the rows via raw curl with the same anon key. Root cause unidentified. Caused mediaProcessed=0 despite discovered.pending=59. | For newly-added text columns being filtered in cron hot-path queries, prefer fetch-all-then-JS-filter when the result set is small (~thousands of rows). Same fix applied at getInstagramRegistry, getShortsRegistry, founder-report. Don't debug client-quirk root cause in production hot-path code — annotate with the explanatory comment block from 88d6a92.
[2026-05-15] | syncOneMedia built the post object then called fetchCommentsForMedia, which throws on transient IG errors / rate limits during the bootstrap burst. The orchestrator's outer try/catch caught the throw and silently discarded the already-built post along with the comments. 35 of 52 expected today rows lost. | When a function constructs multiple results and then performs side effects, wrap each side effect in its own try/catch so a downstream failure can't discard upstream results. Especially in cron orchestrators where partial success > total loss.
[2026-05-17] | D4 keying unification (clip_code → clip_details_code) shipped in 4 commits because the first attempt only updated producers and lookups, then the second pass missed the per-consumer map POPULATION sites, then a third pass missed the JSX RENDERING sites. Two regressions surfaced over two browser verification cycles. | Keying contract has FOUR sweeps, not three: (1) PRODUCER (db function `map.set(key, ...)`), (2) LOOKUP (`totalsMap[key]`), (3) POPULATION (consumer useEffect `map[key] = t`), (4) RENDERING (JSX `{post.clip_code}` labels). When changing a key strategy, grep ALL four classes in the same audit. The rendering sweep is the easiest to skip because it doesn't break tests — it just shows wrong labels. Use shared helpers (`clipKey()`, `displayClipCode()`) so the contract is enforced by import, not by discipline.
[2026-05-17] | Supabase 1000-row response cap bit three separate times in the same session — getAllPostsByDate without bounds returned only oldest 1000 ("All Time" stuck in 2023–mid-2024), getTotalViewsPerClip extension shipped without pagination (~80% undercount on aggregates), then on re-verification the same function still showed wrong totals because pagination lacked stable ordering. | Default to pagination for ANY aggregation function reading posts/studio_snapshots — even if today's bounds keep it under 1000. Pattern: PAGE = 1000 + .range(from, from+999) loop + stable .order() + break-on-short-page. Pagination without ordering is undefined behavior (Postgres can skip/duplicate rows across pages). Same shape as /api/founder-report.
[2026-05-17] | /api/diagnostics internal_consistency check under-reported shorts views by 34% for an unknown duration — its recompute query lacked the PENDING-exclusion filter founder-report applied. Caught only by an opportunistic health sweep, not by anyone reading the diagnostic. | Diagnostics that aren't read aren't diagnostics. If a check is supposed to be load-bearing, wire critical RED statuses into a notification path (Slack, email, dashboard banner) before considering the feature shipped. JSON-on-endpoint is a manual inspection ritual, not a system. Also: consistency-check queries MUST mirror the filter set of the queries they verify — otherwise drift detected is filter-mismatch noise, not real drift.
[2026-05-18] | Recommended a posts.url backfill in response to a modal "Video URL not set yet" symptom without first tracing what column the modal actually reads. The modal reads clip_details.video_url (86.7% NULL), not posts.url — the backfill was correct hygiene but couldn't have fixed the symptom. Discovered only after user verified the fix in browser. | Before recommending any backfill or data fix from a UI-symptom report, grep for the exact rendered text or trace the data flow from the rendering component back to its source column. "Symptom names a column" is the most expensive class of assumption.
[2026-05-18] | Initial 30d ground-truth query used window [today-30d, today] interpreted as inclusive (31 days), reporting a false ~3% drift vs UI. UI's presetRange() uses UTC date math (toISOString().slice(0,10)) producing a true 30-day rolling window that can shift ±1 day depending on view time. | When ground-truthing UI numbers, read the date-construction function in the consumer view and replicate its exact bounds — don't recompute the window from the preset label or rely on inclusive/exclusive intuition.
[2026-05-18] | End-of-session check flagged YT cron as "3 days stale" without first cross-referencing the known YouTube Analytics API 2-3 day reporting delay already noted in primer.md for 2026-05-17. YT cron was running fine; stat_date trailing today by 2-3 days is intrinsic to the API. | Before escalating data staleness, check primer.md and CLAUDE.md for known platform reporting delays. YT Analytics has an intrinsic 2-3 day reporting lag — stat_date appearing N-3 days behind today is normal, not a cron failure.
[2026-05-18] | Pagination without `.order()` bit a third time this session on `getTotalViewsPerClip` — caused the Dashboard 138.8K vs Platforms 138.7K divergence (about 95 views skip/duplicate across the 5-page fetch). CLAUDE.md already had the rule; I constructed the paginator from memory and forgot the order clause. | When adding any paginated `posts`/`studio_snapshots` reader, copy the SHAPE from an existing-correct one. Founder-report's pattern is the canonical reference: PAGE constant + `.range(from, from+PAGE-1)` + `.order('id', { ascending: true })` + break-on-short-page. Don't write paginators from memory.
[2026-05-18] | Scheduled diagnostics-alert cron returned 401 from its secondary fetch to `/api/diagnostics` while manual curl from a local terminal succeeded. Root cause: Vercel cron routes scheduled invocations to a protected alias domain (Protect Cron Jobs); the primary request bypasses via Bearer CRON_SECRET, but a bare secondary fetch back to `${origin}` hits the same protected alias with no Bearer and gets the HTML auth wall. Manual curls work because they hit the public production URL, not the protected alias. | Always propagate `Authorization: Bearer ${process.env.CRON_SECRET}` on cross-route fetches inside Vercel crons when deployment protection is enabled. Build cron-internal HTTP calls with the same auth that bypasses protection on the primary tick. Better long-term: extract shared logic into a lib so the cross-route fetch isn't needed at all. (SUPERSEDED 2026-05-19 — Bearer-propagation hypothesis empirically failed; default directly to lib extraction.)
[2026-05-18] | Slack diagnostics alerter (32da918) — design notes worth preserving. Single `text` field, no blocks/attachments, no @-mentions: keeps payload simple, dodges Slack's block-kit rate limits, works with the minimal Incoming Webhook URL setup. KNOWN_RED_PATHS mute list is structurally essential — any diagnostic that has BOTH live-RED checks (e.g. cron freshness, drift) and structural-RED checks (e.g. deleted-scraper fallout) must explicitly mute the structural ones or the alerter spams every cron tick. Include the diagnostics URL in the message body so the recipient can click through to the JSON. | When building a status alerter on top of an existing diagnostics endpoint, identify the structural-RED set first (paths that read RED by design, not by failure) and bake the mute list into the alerter, not the diagnostics endpoint itself — the endpoint stays an accurate state report; the alerter applies notification policy on top.
[2026-05-18] | On task 1b ("expand IG sync to pull ig_reels_avg_watch_time"), I overshot scope and also rewrote the Dashboard Avg View Duration tile — dropping the "YouTube only" caption and the IG → N/A override. User had to redirect: "ship the data plumbing, leave the UI label alone." Required a soft-reset + recommit. | When a task is explicitly framed as "plumbing only" or "data layer only" or "ship X, leave Y alone," resist optimizing adjacent code that the change makes possible but isn't part of the brief. UI implications of a data fix are a separate decision the user wants to make later, not bundled in.
[2026-05-19] | The 2026-05-18 lesson "propagate Bearer CRON_SECRET on cross-route fetches" was empirically wrong. 1 PM and 7 AM UTC scheduled ticks both 401'd despite the Bearer header from 4e80c0f. Vercel deployment-protection rejects ALL secondary fetches to the protected alias regardless of Bearer. Manual curls succeeded only because they hit the public URL, not the protected alias. | For any computation that needs to be callable from both an HTTP route AND a cron, extract it into a runtime-agnostic lib (no Request/NextResponse/header reads) from the start. The HTTP route is a thin wrapper; crons call the lib directly. Avoids the deployment-protection trap entirely. Precedent: src/lib/diagnostics.ts (cf3b8b5). Open watch item: same pattern may extend to /api/founder-report if internal_consistency consistently 401s in cron context.
[2026-05-19] | After cf3b8b5 deployed, a 12:00 UTC scheduled tick produced a Slack alert "/api/diagnostics returned 401" — a string that no longer exists in the new bundle (the refactor deleted that branch). cf3b8b5 was committed at 17:09 UTC, ~5h AFTER the 12 PM tick fired. Saved a code investigation by checking message format vs current source first. | When an alert fires after a fix lands, compare the alert message text against current source before debugging code. If the text matches strings that exist only in pre-fix source, it's a pre-deploy tick — confirm timing, don't re-debug.
[2026-05-19] | Manual curl returned clean 200 on /api/cron/diagnostics-alert post-cf3b8b5-deploy. That curl proves the in-process call works on the public URL — it does NOT validate cron-context behavior on the protected alias where scheduled crons actually run. The two paths differ at the Vercel deployment-protection layer. | A manual curl from your terminal validates code correctness, not cron-context behavior. To verify a fix specifically tied to Vercel deployment protection or cron-alias routing, either wait for the next scheduled tick or use the "Run Cron Job" button in the Vercel dashboard.
[2026-05-19] | Three TS target gotchas broke npm run build in one Commit-4 session: BigInt literals (0n), inner function declarations inside arrow functions, and for...of on Maps. | Default to: number (not bigint) for IDs; const arrow functions for inner helpers; map.forEach(callback) instead of for...of on Map/Set. The tsconfig target is below ES2015/ES2020 in this repo.
[2026-05-22] | Manual SQL Editor "migration applied" claim turned out to be empty — neither pg_proc nor pg_trigger had the new objects. Verification firing failed for 30 min before catalog-query revealed the trigger absent. | After any manual DDL, run a one-liner pg_trigger / pg_proc / information_schema query confirming the object exists BEFORE any end-to-end verification.
[2026-05-22] | posts.updated_at had DEFAULT now() but no BEFORE UPDATE trigger. ON CONFLICT DO UPDATE left it frozen at INSERT time. IG cron's same-day UPDATEs didn't bump it, masking 3 days of "successful" runs with no posts.updated_at movement. | DEFAULT now() fires INSERT-only. For UPDATE-aware "last touched" semantics, add a BEFORE UPDATE trigger to the table. Don't assume a column default gives last-touched semantics.
[2026-05-22] | cron_runs.rows_processed > 0 + status=success was treated as "writes landed" but the upsert was UPDATE-only and the trigger gap meant nothing in posts had its updated_at moved. Diagnostics showed cron_health green via cron_completion while last_instagram_sync ratcheted to "19h ago" silently. | "Cron ran" and "writes landed" are distinct invariants. Build a backstop diagnostic check that correlates cron_runs windows against posts.updated_at movement for the affected platform. Priority-1 next session.
[2026-05-25] | Edit on JSX inside a `.map()` callback truncated the file's structure because new_string included closing tags meant for a different scope (the JSX after the special-case if-branch). The old_string/new_string boundaries silently destroyed everything from the Unique Viewers `<p>` through the end of the surrounding `</div>`. Cost: full file revert via `git checkout HEAD -- ...` and clean re-do. | When using Edit on JSX inside an array map's branching renders, verify the diff (`git diff`) before treating the edit as successful — JSX closure scope inside `.map()` is non-obvious and old_string/new_string boundary mistakes silently destroy structure outside the intended replacement. Conditional renders interleaved with element JSX are especially hazardous.
[2026-05-25] | Explore agent reported YT discovery cron runs "every 6 hours per vercel.json" — actual schedule is `"0 14 * * *"` (daily at 14:00 UTC). The agent inferred the frequency from youtube-sync.ts's call cadence instead of reading vercel.json directly. Caught and corrected in the report, but a less attentive consumer would have built mental model on the wrong cadence. | For any cron-schedule claim from a subagent, verify directly against vercel.json — don't trust subagent inferences about schedule frequency. Same pattern applies to other config-file-of-truth artifacts (database constraints, RLS policies, env vars).
[2026-05-25] | Phase 2B re-keyed `posts.clip_details_code` from `PENDING-IG-{id}` → `MBM###-CLIP-###` for 7 IG content_ids without auditing the IG cron's upsert contract. The cron's `getInstagramRegistry()` still returned PENDING-IG entries (because `clip_details.instagram_content_id` lives only on PENDING-IG rows, not on MBM rows post-Phase-2B). Cron tried to upsert with stale `PENDING-IG-{id}` clip_details_code, missed the re-keyed posts row, fell back to INSERT, hit `posts_contentid_platform_statdate_key`. 9.5h IG sync gap before the 00:00 UTC heartbeat caught it. Initial bug report stated "`clip_details.content_id` is dual-use (YT video_id AND IG media_id)" — actual schema has a dedicated `instagram_content_id` column and content_id is NULL on PENDING-IG rows. | When migrating a key column in `posts`, enumerate ALL consumer crons that key off the same column BEFORE shipping. Shorts side of Phase 2B worked because YT cron's registry keys off `content_id` (stable). IG side broke because IG cron's upsert keys off `clip_details_code` (re-keyed). Also: before drafting revert/fix SQL, verify the stated schema against actual columns — a user-supplied schema claim is a hypothesis, not a fact, especially mid-incident.
[2026-05-29] | Verified against a known upload (nBCgJxAlVJE, expected filename MBM028-CLIP-003.MOV) that `fileDetails.fileName` is no longer returned by the YouTube Data API even with sufficient scope (youtube.force-ssl granted) — the field is gone server-side, not scope-gated. Combined with `snippet.tags` being empty on every upload to this channel, BOTH upload-side auto-map signals are dead. | `fileDetails.fileName` is a dead mapping signal — don't plan auto-discovery around it and don't recommend re-auth to "fix" it. Mapping is manual. The ONLY sanctioned way to re-key a clip mapping is the atomic `map_clip(p_code, p_yt_video_id, p_ig_content_id)` plpgsql function (supabase/migrations/20260529_clip_mapping_integrity.sql) — it frees each UNIQUE column before claiming it on the MBM row and RAISEs on any half-state so a partial mapping can never persist. Never re-key `clip_details_code` across PENDING/MBM rows with raw multi-statement SQL again (that caused the 5/25 incident). `ig_mapping_desync()` is the heartbeat probe for the exact 5/25 cross-row invariant — wired into `schema_integrity.ig_mapping_desync` and NOT muted in KNOWN_RED_PATHS, so any desync row alerts.
